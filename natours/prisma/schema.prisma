generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model Tour {
  id      Int      @id @default(autoincrement())
  name   String   @unique
  rating    Int @default(4)
  price Int
}
